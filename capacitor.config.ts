import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.xuegao.footprints",
  appName: "雪糕足迹",
  webDir: "dist",
  server: { androidScheme: "https" },
};

export default config;
